سیستم مدیریت فروش و موجودی (FastAPI + Vercel)

این پروژه یک سیستم ساده و کاربردی برای مدیریت محصولات، موجودی انبار و ثبت فروش است که با Python و FastAPI پیاده‌سازی شده و قابلیت اجرا هم به‌صورت محلی (Command Prompt / PowerShell) و هم به‌صورت آنلاین روی Vercel را دارد.

 نسخه آنلاین پروژه

لینک دپلوی‌شده روی Vercel:
👉 https://7-d4d8.vercel.app/

 معرفی پروژه

این سیستم امکانات پایه و ضروری برای مدیریت موجودی و فروش را فراهم می‌کند و برای استفاده به‌عنوان نمونه‌کار Backend، پروژه دانشجویی یا تمرین معماری Serverless بسیار مناسب است.

 امکانات پروژه

افزودن محصول جدید

ثبت فروش و کسر خودکار موجودی

جلوگیری از فروش در صورت کمبود موجودی

ذخیره اطلاعات محصولات و فروش

API مستندشده با Swagger (/docs)

سازگار با معماری Serverless

قابل دپلوی روی Vercel

توجه: به‌دلیل معماری Serverless در Vercel، دیتابیس SQLite به‌صورت موقت در مسیر /tmp اجرا می‌شود و برای محیط Production آماده اتصال به دیتابیس‌های دائمی مانند PostgreSQL یا Supabase است.


 نصب و اجرای پروژه (محلی)
نصب وابستگی‌ها
pip install -r requirements.txt

اجرای پروژه
uvicorn main:app --reload


حالا پروژه روی آدرس زیر در دسترس است:

👉 http://localhost:8000
